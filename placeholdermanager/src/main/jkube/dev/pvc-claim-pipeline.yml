kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: placeholdermanager-pipeline-claim
  namespace: placeholdermanager-dev
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: ocs-storagecluster-ceph-rbd
  volumeMode: Filesystem
status:

